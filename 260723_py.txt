only-pull-me폴더 -> Open Git Bash here -> git pull origin master
**소스코드 업데이트(다운로드)는 작업(수업)시작 전 기본
오류가 생겼을경우, only-pull-me폴더 지우고 - clone을 다시하면 됨
-------------------------------------------------------------------------------------------------------------------------
내용복습

<키워드/예약어>
Boolean - False, True
None
논리연산자 - and, or, not / 비교 연산 is
포함테스트 - in
del - 변수 삭제(정의된 변수 삭제)
ex) print = 'hi'; del print
def - 함수 정의 / return
class - 클래스 정의

조건문(~일때, ~를실행) - if, elif, else / match, case
반복문 - (반복키워드)for, while / (제어키워드)break, continue, pass
모듈 - import, from, as
에러처리 - try-except, finally / raise

계산했을때 값이 딱 나온다 = 표현식
-------------------------------------------------------------------------------------------------------------------------
2_3_자료형_시퀀스형
군집형-문자열(String)타입
Single quotes( ' )자주사용
여러줄은 삼중따옴표 : '''~~'''

맨마지막줄의 결과는 주피터노트북의 기능으로 보여줌(웬만하면 print하는 습관을 들일 것)

이스케이프 문자(\, 역슬래시)를 통해 문장부호를 처리 할 수 있다.
-------------------------------------------------------------------------------------------------------------------------
git add 파일
git commit -m "메시지"
git push origin master







