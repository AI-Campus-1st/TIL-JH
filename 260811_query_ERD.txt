[ERD 설계]
- 계층을 나누는이유 ? => 한 테이블에 다 넣으면 편하지만 고칠 수 없다.
1. 재적재 가능성   2. 장애 추적   3. 역할 분리   4. 성능 격리

- 네 개의 계층
1) RAW(적재) : 변환하지 않는다. 실패하지 않는것이 최우선(전부 문자열, 제약 최소화, PK는 대리키)
★2) CLEAN(정제) : 타입확정, 이상치 처리, 정규화, 불량행 격리(ERD의 핵심)
3) MART(집계) : 분석 질문에 맞춰 미리계산해 둔다.
4) DASHBOARD : 조인 없이 한 번에 읽도록 넓게 편다.
