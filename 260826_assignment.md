### 문항1. 순차·스레드·프로세스 속도 비교
> 같은 작업을 세 가지 방식으로 실행해 소요시간을 비교하시오.

- 대상은 로컬 Flask 서버를 활용하세요.
- 세 방식
    1. 순차 반복문
    2. `multiprocessing.dummy.Pool(n).map` (스레드)
    3. `multiprocessing.Pool(n).map` (프로세스)
- 워커 수 `1 · 3 · 5 · 10` 을 바꿔가며 측정해 표로 정리
- 측정 결과를 보고 두 방식의 차이가 어디서 오는지 2~3줄로 설명할 것 (힌트: 대기 시간, 풀 생성 비용 · 메모리 · 직렬화 제약도 함께 생각해볼 것)

`bench_server.py` — 지연 응답을 흉내내는 로컬 서버
```
from flask import Flask, jsonify
import time, random

app = Flask(__name__)

@app.get("/item/<int:n>")
def item(n):
    time.sleep(random.uniform(0.3, 0.6))     # 네트워크 대기를 흉내낸다
    return jsonify({"id": n, "name": f"item-{n}"})

if __name__ == "__main__":
    app.run(port=5000, threaded=True)
```


### 답안

|방식|워커 수|소요 시간|
|---|:---:|---:|
|순차 반복문|-|4.12초|
|스레드|1|4.46초|
|스레드|3|1.83초|
|스레드|5|1.13초|
|스레드|10|0.61초|
|프로세스|1|4.66초|
|프로세스|3|2.18초|
|프로세스|5|1.38초|
|프로세스|10|1.30초|

```
스레드는 대기 시간이 있는 작업에서 다른 작업을 동시에 처리할 수 있어 시간이 크게 줄었다.
프로세스도 동시에 처리할 수 있지만, 프로세스를 생성하고 관리하는 비용이 추가로 발생했다.
이번 결과에서는 스레드가 프로세스보다 전반적으로 더 빠르게 나타났다.
```