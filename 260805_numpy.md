# Numpy : 넘파이
### : 숫자,행렬을 다룰때 편하게 사용할 수 있는 모듈
### : Python에서 수치 계산을 위한 라이브러리

- np.arange -> 파이썬의 range와 비슷
- np.reshape -> 배열을 다시 하는것 (1행-> 3행5열로 재분배)
- np.array -> 다차원 슬라이싱
- 슬라이싱 후, 특정위치의 값을 바꿀 수도 있음
- axis (축) : x axis (x축) / y axis (y축)

#### 브로드캐스팅 : 차원이 다른 배열끼리의 연산을 도와주는 기능

>  np.linspace(시작, 끝, 값갯수, endpoint=True)
    - 그래프 그릴때 촘촘한 값을 줄 수 있음  
    -  endpoint는 기본적으로 True(끝값포함), False면은(끝값미포함)

> 최소값, 최대값위치 메서드 많이 쓰임  
- np.argmin(x) # 최소값 위치  
- np argmax(x) # 최대값 위치
